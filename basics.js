// react and nextjs are frameoworks
//browser only runs html js and css and WebAssembly
//line by line execution compile at runtime
//runnning is faster when compilation is done before running
//js does this one thing together with the running so it runs
/// bit slower in comparison
// to the cpp
//pronetoruntimeerrors
// CPP type casts the bool to a number
//slow becz of garbage colector
  
var a=1;
a = "hammm";
a= true;




